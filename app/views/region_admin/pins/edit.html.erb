
<h2>Edit map details</h2>

<% form_for :pin, :url => region_admin_region_pin_path(@pin.region, @pin), :builder => LabelledBuilder, :html => {:onsubmit => "map.writeLatLong('pin_lat', 'pin_long'); return true;", :method => :put} do |f| %>
	<%= f.hidden_field :region_id %>
	<%= f.hidden_field :lat %>
	<%= f.hidden_field :long %>	
	<%= f.hidden_field :country %>	

	<h3>Pin Numbering</h3>

	<p>These two fields only need to be modified if you want to force the pin to display a particular
	number and colour, in order to make it match a physical map.</p>

	<%= f.text_field :code, :label => "Pin Number" %>
	<%= f.select :colour, Pin::COLOURS.map(&:to_s), :include_blank => true, :label => "Pin Colour" %>

	<%= render :partial => 'pins/form', :locals => {:f => f} %>
	
	<%= form_footer(:pin, :cancel_link => url_for(:action => :index)) %>
<% end %>

